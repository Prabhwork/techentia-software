rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Allow full read/write on all documents for now
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
